function osc-help {
    printf "Available functions include (all defined in common-scripts submodule):\n those defined in obs/build.sh, namely: * oscbot (build for Tumbleweed)\n * oscba (build for Arch Extra)\n"
}
